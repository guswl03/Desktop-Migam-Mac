# Desktop Pet 작업 규칙

이 저장소에서 작업하는 모든 세션은 실제 구현 전에 다음 문서를 순서대로 읽는다.

1. `docs/01-scope-and-priorities.md`
2. `docs/17-session-handoff.md`
3. `docs/13-progress-board.md`
4. 현재 단계에 해당하는 `docs/06-ai-agent-work-orders.md` 작업 블록

## 세션 시작 규칙

- `docs/17-session-handoff.md`의 현재 상태와 다음 작업을 실제 코드 및 Git 상태와 대조한다.
- 기존 코드를 보존하고, 완료로 기록된 작업을 근거 없이 다시 만들지 않는다.
- 문서와 코드가 다르면 코드·검증 결과를 기준으로 차이를 먼저 기록한다.
- UI는 React를 추가하지 않고 Vite + 순수 TypeScript + DOM API로 구현한다.
- 창 개입은 기본 off이며 보호 여부가 불확실한 창은 조작하지 않는다.

## 세션 종료 규칙

작업량과 관계없이 종료 전에 반드시 다음을 수행한다.

1. 가능한 포맷·테스트·빌드 명령을 실행한다.
2. `docs/13-progress-board.md`에서 단계 상태와 테스트 기록을 갱신한다.
3. `docs/17-session-handoff.md`를 현재 상태로 덮어쓴다.
4. 완료 기능, 변경 파일, 검증 결과, 차단 요소, 정확한 다음 작업을 기록한다.
5. 실행하지 못한 검증은 성공으로 표시하지 않고 이유와 필요한 명령을 남긴다.

## 완료 판정

- 코드 작성만으로 단계를 완료 처리하지 않는다.
- 해당 작업 게이트와 관련 자동 테스트가 통과해야 `완료`로 표시한다.
- Windows에서 직접 확인해야 하는 항목은 자동 검증과 별도로 남긴다.
