{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base capability for the desktop pet windows.",
  "windows": ["pet", "card", "timer", "settings"],
  "permissions": ["core:default"]
}
